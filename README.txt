After checkout:
- install packages from requirements.txt
- add pypy/rpython checkout to project PYTHONPATH
- install libmysqlclient-dev (for header files)
- install libpcre-dev (for header files)
- install librhash-dev (for header files)
- install zend php5 interpreter
- set /short_open_tag = on/ in php.ini
